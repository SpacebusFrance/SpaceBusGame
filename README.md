# SpaceBusGame

Install dependencies using the script:

    ./install_dependencies.sh

In case you have a libGL error, uncomment the conda line in the above script.

# Scenarios

The scenarios' texts are in:

    data/gui/texts/text.csv